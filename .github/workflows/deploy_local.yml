name: Deploy Laravel Sail (Local)
on:
  push:
    branches: [ "main" ]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy (Local) con Sail
        run: |
          set -e
          cd /mnt/c/Users/sande/Desktop/DAW/Curso\ 2025-26/DAW\ –\ Desplegament\ d\'aplicacions\ Web/PREPARACION\ EXAMEN/U5
          git pull
          ./vendor/bin/sail down
          ./vendor/bin/sail up -d --build
          ./vendor/bin/sail artisan migrate --force
